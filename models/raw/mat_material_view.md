--  materialized view does not support in snowflake
-- {{
--     config(
        
        --        materialized= 'materialized_view'
--     )
    
--     }}

--     select * from dbt_sql.raw.orders;